Automation Framework :>>>>>>>>>>>>>>>>>>>>>>we organize automation project files/folders in structured manner.Objectives:>>>>>>>>>>* Re-usability(use already develop code)* Maintainability(add extra or changes or remove)* Readability(Every one can understand)Frameworks:>>>>>>>>>>>* Built-inEX: test NG, junit, cucumber, robot framework etc..*Customized Framework(user defined)ex: modular framework, data driven, keyword driven, hybrid driven etc..only bult-in frameworks or without we cannot  achieve framework automation. So along with bult-in we can use customized frameworkwe can combining multiple things in hybrid framework.*why hybrid framework automation?A) when ever we are working in opensource tools.so if it is not supporting something else so wee need to integrate it.so that's why hybrid is preferable.*)can you achieve 100 percent automation?A)100 Test cases---90 automatable 10 cannot automatable so here answer which are all automatable that we can automate everything then it is called 100 percent automation.Phases/stages:>>>>>>>>>>>>>1)Analyzing Application-Number of pages-what are all elements/how/type-what to automate/what we cannot automate2)Choose test cases for automation based on prority(colors, font, images, captchas cannot automate)	p1-sanity test cases	p2-data driven test cases/retests	p3-Regression test cases	p4-Any other cases3)Design and development of framework (framework will design by the senior people mostly)4)Execution - local(write in eclipse and execute in eclipse), remotely(execute in some other systems, environments, Jenkins)5)Maintenance.E-Commerce domain>>>>>>>>>>>>>>>>>>1)frontend operations(customers/users)-register-login-search product etc...2)Backend operations(admins/backend teams)-product info-customers-orders-store etc..note : Intail Stage we started developing of e commerce because of demo application un stability we cannot proceed further testing sot hen we will move to the below application.Orange Hrm Application:>>>>>>>>>>>>>https://opensource-demo.orangehrmlive.com/hierarchy of framework:>>>>>>>>>>>>>>>>>>>>>>page objects -----> Test case ------> TestNG.xml --------> pom.xml ----->  Git -----> GitHub ------> JenkinsTest Cases under utilities file , test data file, resource file >>>>>>>> {development}TestNG.xml and pom.xml under  ----> Reports and logs. >>>>>> {Execution}final push to git >>>git hub>>>>>>>Jenkinsstarting development of automation framework :>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>create a maven project and give group id and artifact idAdd all dependencies in pom.xml filecreate packages under src/test/java {pageObjects, testCases, utilities}Test data files we cannot create under src test resources even thow it is resources we cannot create because src is purely java related so we cannot create normal filescreate logs, reports, test data, screenshots folders under project level.In page object class we have 3 parts majorly@ constructor@ web element@ ActionsIn one test case we have multiple page object classes.Steps to follow in detailed:>>>>>>>>>>>>>>>>>>>>>>>>>>>>* **create a project and add all dependencies in pom.xml*** * **create a page object package*** create a base page under page object package* initiate driver and page factory* create a page object model* call super class driver* find web elements* find action methods* * **create a test cases package*** create a base test case under testcases* add driver initialization and action methods which is required for all test cases* close driver and also some common actions performed by all test cases* create a test case and extend the base class* create an object of base pages and pass the driver* call the action methods which we want.* * **Adding logs to the test case.*** Logging -record all the events in the form of text.* events--opening browser clicking and perform operations that operations called events.* Application logs--if user preforming anything in the application backend log files will generate* Automation logs--what are all the actions preforming in the application through automations those all action log into the logfiles.* some test cases will pass in QA but it work in dev so then they will ask log files* Log Levels-- All --> trace --> debug --> info --> warn--> Error--> Fatal---> Off* set log level ==All then you will get all lower level of logs* Set log level ==Trace  then you will get debug, info, warn, error, fatal everything* set log level ==debug the you will get info, warn, error, fatal everything* set log level ==off then you cannot get any logs. like this it will work upper to lower* Appenders -- where to generate logs(console/file) file is more preferable.* so log file decided the type of logs or which level of logs and appenders will decided where to generate the logs* Add log4j-core and lo4j-api to pom.xml and create and add lo4j2 xml file to src/test/resources* In this lo4j2.xml file appender section is there .here you can specify appender reference where we want logs in file or console or both and also root * level which level of logs you want. and property tag will refer the exact log file location(./ is current project location and logs is the file name.** **Run tests on different browsers*** create one cross browser testing.xml file and inside that create multiple test methods and add which are browser methods you want under test method by using parameter tag name="browser" value="chrome" then one more important add parallel=tests in suite tag.* in base class add browser by using parameter annotation and pass it is a variable to setup method and after write switch case statement and add  default and return statement.* * **Read data from properties file.*** add config.properties file to src/resource folder and update base class.** Data Driven Login Test *** Add excel utilityfile.java(code we need to write)* First add test data file in separate folder**Grouping Test Cases**** we can specify groups for test cases along with base test case also that means setup method and tear down methods.* if you give groups to our page class @test(groups="sanity) then you are extended the base class but still it cannot excute the base class because you nedd to give that group names to our base class methods also.**ExtenReport****Add ExtentReportManager utility file to under utilities packageIn that extent report we have done all operations for reports.*create OnStart method1)Add timestamp by using SimpleDateformat method2)add report name along with timestamp.3)By using ExtentSparkReporter set document title, set report name, set theme4)By using ExtentReports  we can print the system information in the reports(os, browser, application name ,module name ,environment, any groups in xml etc)5)Attach this extentReport to sparckReporter6)In onTestSucess method. if the test case is pass we are getting the class and group name from the result by using methods(grtmethod and getclass). and print one logger msg "executed successfully"7)same thing fro onTestFailure.8)Report got generated and will automatically open with timestamp.**How to run only failed test cases again.**in our project level we have folder called tetsng-failed.xml in that we have an xml with all failed test cases so simply we can run that file. that file should be there still next time it is pass also upto next failed xml**Adding our project to GITHUB***dependencies == download required jar for project*plugins == to compile and run the project.1)maven compiler plugin2)maven surefire plugin*Add maven and surefile plugins in pom.xml*Add your xml file in that surefile then based on that xml file(<suiteXmlFile>testng.xml</suiteXmlFile>)like this you give n number of xml files name it should which file need to be executed.**Install Maven software**why this maven software means -->we are coming out from eclipse.C:\Program Files\apache-maven-3.9.14\bin --path of the maven.add this path in environment variablesmvn -version execute this in command prompt.current project location--> C:\Users\DELL\eclipse-workspace\Orangehrmwrite cd C:\Users\DELL\eclipse-workspace\Orangehrm this will change the directory.write C:\Users\DELL\eclipse-workspace\Orangehrm>dir   -->this will give all the files present in our project.write C:\Users\DELL\eclipse-workspace\Orangehrm>mvn clean test	--->This will run our project how mvn test in eclipse and also this clean is optional if you want clean old executions and all you will use otherwise not required*pom.xml--->mvn test---->bat file*pom.xml--->command prompt by using mvn test(this will also execute what are the xml file name in pom.xml dame file it will execute)*By using bat file also we can execute. only this two lines as it is 	cd C:\Users\DELL\eclipse-workspace\Orangehrm	mvn test**Final execution**Pom.xml-->testing.xml--->test Cases--->pageObject---					utilites					testdata.**Push the code into Git**1)every one write code in our local and save in their local repository .and after that they send to global repository.Git-local GitHub-global or remote repository.*sending the code to GIT then it is called commit. After that sending the code from Git to GitHub then it is called push*see once development team push the code to GitHub then devops team will get that code from repository and they will perform some operations like build creation, run automation(not entire major flow), build certification.*so testing team also done the same process git and from git to git hub.*see once they get code from dev team and testing team the do will do build creation and run automation and build certification. Devops team also will used maven.*Jenkins also pull the latest code from github and it will execute daily.pre-requesties for git :>>>>>>>>>>>>>>>>>>>>>>>>1)Git installation2)create an account with github and create remote repository then capture url.**work flow of Git and Github**1)git init	-->it will create a new local repositroy2)git config --global user.name "bhargav"	git config --global user.email "bhargavpunuru@gmail.com"	--->this will create only one time3)git status 	-->it will give the status files or folders are tracked or not .if not all files show in red color4)git add -A ---> this will add all files into the satging.	git add tetsng.xml or git add reports 	so here we can add specific file or folders also	git add *.java -->this will add all java files5)git status	--> after running this "git add -A" we can run this status again the you can see all files are in green color so these files are tracked.6)git commit -m "commit message"	---->commit the code into local git repository. commit msg nothing but regression testcases like that. After this all the files are in white color.7)Again give command like this git status then it will show nothing to commit.	**red color==untracked, green color==tracked files, white color=added in git repository**8)connect local repository ton remote repositoryrepository url=https://github.com/Bhargav-QA/Orangehrm.gitgit remote add origin https://github.com/Bhargav-QA/Orangehrm.git9)Push the files into remote repository.	git push origin mastertoken =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10)suppose i have added a new test case and done some modification in existing test case in local repositorynow directly add and commit and push because everything already created.11).suppose i have added a new test case and done some modification in existing test case in remote repository.git pull https://github.com/Bhargav-QA/Orangehrm.git12)Clone new repository to local systems.select the repository and click on code the you we will get repository link.where ever you want to clone got to that location in local file explorer and right click open GitHubgit clone "https://github.com/Bhargav-QA/Orangehrm.git"	--->execute this statement you will get this and add to eclipse.13)only first time we will use clone and once project imported from remote repository the we will use pull command.14)suppose tester t1,t2 is pushing the code into GitHub must be have some common files an all.so then conflict will happen.so it is not happen while committing to github why because each tester having their own branch url in GitHub like branch1,baranch2 etc. whenever some technical people like seniors or admins can merge our code and push to the master then the conflict will happen then they will resolve it.so final working code is push to the master.